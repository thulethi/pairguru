- @movie.comments.each do |comment|
  .comment
    .col-md-8
      %i.body= comment.body
    .col-md-4
      %p
      = time_ago_in_words(comment.created_at)
      ago by
      %strong= comment.author.name
